language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-browser-project' ]; then cd .. && eval "mv $currentfolder generator-browser-project" && cd generator-browser-project; fi
